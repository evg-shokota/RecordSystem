# Шаблони документів — Бриф для розробки

Цей файл описує які шаблони потрібно створити в системі.
Після заповнення — скажи Клоду "опрацюй шаблони" і він створить їх у редакторі.

---

## Як заповнювати

Для кожного шаблону:
1. Вкажи **тип документа** (з таблиці типів нижче)
2. Вкажи **назву** шаблону
3. Прикріпи **скан або фото** зразка (якщо є) — поклади в `C:\Rechova\Скріншоти\`
4. Або опиши **структуру** вручну (що де стоїть)

---

## Доступні типи документів

| Код | Назва |
|-----|-------|
| `invoice` | Накладна (вимога) |
| `rv` | Роздавальна відомість |
| `attestat` | Речовий атестат |
| `write_off` | Акт списання |
| `exploit_act` | Акт введення в експлуатацію |
| `registry` | Реєстр документів |
| `custom` | Довільний шаблон |

---

## Доступні змінні (shortcodes)

### Документ
| Код | Що підставляє |
|-----|--------------|
| `{{invoice_number}}` | Номер документа |
| `{{invoice_date}}` | Дата документа |
| `{{base_document}}` | Підстава |
| `{{total_sum}}` | Загальна сума (цифрами) |
| `{{total_sum_words}}` | Загальна сума (прописом) |
| `{{valid_until}}` | Дійсна до |
| `{{doc_date_full}}` | Дата прописом |
| `{{current_year}}` | Поточний рік |

### Одержувач
| Код | Що підставляє |
|-----|--------------|
| `{{recipient_name}}` | ПІБ одержувача |
| `{{recipient_rank}}` | Звання одержувача |
| `{{recipient_unit}}` | Підрозділ одержувача |

### Підписанти
| Код | Що підставляє |
|-----|--------------|
| `{{chief_name}}` | Начальник служби — ПІБ |
| `{{chief_rank}}` | Начальник служби — звання |
| `{{chief_tvo}}` | ТВО позначка |
| `{{given_name}}` | Здав — ПІБ |
| `{{given_rank}}` | Здав — звання |
| `{{received_name}}` | Прийняв — ПІБ |
| `{{received_rank}}` | Прийняв — звання |
| `{{warehouse_name}}` | Нач. складу — ПІБ |
| `{{warehouse_rank}}` | Нач. складу — звання |
| `{{clerk_name}}` | Діловод РС — ПІБ |
| `{{clerk_rank}}` | Діловод РС — звання |

### Організація
| Код | Що підставляє |
|-----|--------------|
| `{{unit_name}}` | Назва частини |
| `{{service_name}}` | Назва служби |

### Таблиці
| Код | Що підставляє |
|-----|--------------|
| `{{table:items_list}}` | Таблиця позицій майна (рядки з БД) |
| `{{table:signatories}}` | Блок підписантів |

---

## Шаблони до створення

<!--
  ІНСТРУКЦІЯ:
  Заповни кожен блок нижче. Можна:
  - Вставити скан/фото документа (назву файлу зі Скріншоти\)
  - Описати структуру словами
  - Намалювати ASCII-схему
  - Написати HTML вручну
-->

---

### Шаблон 1

**Тип:** <!-- напр: invoice -->
**Назва:** <!-- напр: Накладна-вимога Додаток 25 -->
**Група:** <!-- напр: Накладні, або залиш порожнім -->
**За замовчуванням для типу:** <!-- так / ні -->
**Поля (мм):** верх=20, низ=20, ліво=30, право=10

**Зразок / Опис:**
```
[вставити назву файлу скану або описати структуру]
```

---

### Шаблон 2

**Тип:**
**Назва:**
**Група:**
**За замовчуванням для типу:**
**Поля (мм):** верх=20, низ=20, ліво=30, право=10

**Зразок / Опис:**
```

```

---

### Шаблон 3

**Тип:**
**Назва:**
**Група:**
**За замовчуванням для типу:**
**Поля (мм):** верх=20, низ=20, ліво=30, право=10

**Зразок / Опис:**
```

```

---

<!-- Додавай нові шаблони копіюючи блок вище -->
